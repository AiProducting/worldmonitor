name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    uses: Ai-road-4-You/enterprise-ci-cd/.github/workflows/release-tag-only.yml@main
    with:
      bump: auto
    secrets: inherit
